{
  "name": "nuxt-app",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "lint": "nuxt prepare && eslint .",
    "lint:fix": "nuxt prepare && eslint . --fix",
    "typecheck": "nuxt prepare && vue-tsc -b --noEmit",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/content": "^3.8.0",
    "nuxt": "4.1.2",
    "nuxt-auth-utils": "^0.5.25",
    "vue": "^3.5.22",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@cooperco/nuxt-layer-base": "^1.1.0",
    "@cooperco/nuxt-layer-quasar": "^1.0.5",
    "sass": "^1.93.3"
  }
}
